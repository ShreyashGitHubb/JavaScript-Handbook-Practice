// create a constant object in java script can you change it to hold a number leter

const myobject = {
    key: 'initial value'
};

// changing the value of property of an object 
myobject.key = 200; // this is valid

console.log(myobject.key);

// myobject = {newkey : 100};  // this is invalid because we cannot assign the new object to it